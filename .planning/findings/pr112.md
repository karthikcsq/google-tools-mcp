

## Finding 1 (posted 2026-08-21T17:03:30Z)

**Adversarial Review — issue**

Switching setup from shared HTTP back to stdio leaves the persisted HTTP transport active, so the newly-written stdio client entry does not actually start a stdio server.

Failure scenario:
1. Run setup once and choose shared HTTP. `configureTransport()` calls `persistTransportSelection('http')`, which writes `GOOGLE_MCP_TRANSPORT=http` into the user `.env`.
2. Run setup again and choose `One process per client` / stdio.
3. The stdio branch returns immediately from `configureTransport()` and neither updates the persisted `.env` nor overrides `process.env.GOOGLE_MCP_TRANSPORT`.
4. `registerClients()` then writes a normal stdio entry (`node .../dist/index.js`). On the next client launch, `dist/config.js` reloads the still-persisted `GOOGLE_MCP_TRANSPORT=http`, and `dist/index.js` selects HTTP rather than stdio. The MCP client is waiting on stdio, so the repaired entry cannot connect.

The current implementation only persists one direction:
```js
export async function persistTransportSelection(transport, ...) {
    if (transport !== 'http') return;
    await mergeCredentialEnv(..., { GOOGLE_MCP_TRANSPORT: 'http' });
}
```
and the stdio selection exits before any persistence/update.

Smallest fix / acceptance criteria: persist the selected transport in both directions and update the current setup environment consistently. In particular, selecting stdio should explicitly persist `GOOGLE_MCP_TRANSPORT=stdio` (rather than merely deleting it, since a lower-priority cwd/package `.env` could otherwise reactivate HTTP). Add a regression test that starts from a user config containing `GOOGLE_MCP_TRANSPORT=http`, selects stdio, and verifies the resulting client launch resolves to stdio in a fresh process.


## Finding 2 (posted 2026-08-21T17:03:42Z)

**Adversarial Review — issue**

`doctor` and the returning-user setup path treat a valid service-account installation as broken because their auth inspection is OAuth-only, even though the runtime explicitly supports `SERVICE_ACCOUNT_PATH`.

Concrete failure scenario:
- Configure the server with `SERVICE_ACCOUNT_PATH=/path/key.json` (and optionally `GOOGLE_IMPERSONATE_USER`) and no OAuth client ID/secret or `token.json`.
- Runtime auth succeeds via `authorizeWithServiceAccount()` because `authorize()` checks `SERVICE_ACCOUNT_PATH` first.
- `google-tools-mcp doctor` calls `inspectSetup()`, whose `checkCredentials()` only calls `loadClientSecrets()` and whose `inspectToken()` only reads/refreshes OAuth `token.json`. The report therefore adds both `OAuth credentials are not configured` and `OAuth token: missing` and exits unhealthy despite a usable auth configuration.
- `google-tools-mcp setup` has the same mismatch: `runSetup()` uses `checkCredentials()` to decide whether this is a returning installation, so a service-account-only user is routed into the first-install OAuth wizard.

This is not just an unsupported configuration: `dist/auth.js` has a first-class service-account path and the public configuration documents `SERVICE_ACCOUNT_PATH`.

Smallest fix / acceptance criteria: make setup inspection auth-source-aware. When `SERVICE_ACCOUNT_PATH` is configured, validate/report the service-account configuration instead of requiring OAuth client secrets and `token.json`, and let returning setup skip the OAuth wizard when that auth source is healthy. Add doctor/setup tests for a service-account-only configuration with no OAuth files.


## Finding 3 (posted 2026-08-21T17:03:52Z)

**Adversarial Review — issue**

Custom diagnostic log paths can chmod an unrelated existing parent directory to `0700`, and common paths such as `/tmp/server.log` simply fail to initialize because the process cannot chmod `/tmp`.

`initLogFile()` / `initStructuredLogFile()` call `ensurePrivateDirectory(path.dirname(filePath))`, and that helper always does:
```js
fs.mkdirSync(directory, { recursive: true, mode: 0o700 });
fs.chmodSync(directory, 0o700);
```
This is correct for the dedicated default config directory, but `GOOGLE_MCP_LOG_FILE` and `GOOGLE_MCP_JSONL_FILE` explicitly accept arbitrary custom file paths.

Concrete failures:
- `GOOGLE_MCP_LOG_FILE=/tmp/google-tools-mcp.log` reaches `chmodSync('/tmp', 0700)`. A normal user gets EPERM, the catch path warns, and requested logging is disabled.
- If the custom parent is an existing directory the user owns and intentionally shares/traverses (for example `$HOME/shared-logs` at `0755`), starting the server silently changes that directory to `0700`, affecting unrelated files/users.

The new test only creates a fresh dedicated `diagnostics` directory, so it verifies the intended default case but not an existing arbitrary parent.

Smallest fix / acceptance criteria: create/chmod a directory only when this logger owns/creates the dedicated diagnostics directory; for an existing custom parent, leave its mode unchanged and create/chmod only the log file itself to `0600`. Add tests with (1) a pre-existing custom parent whose mode must remain unchanged and (2) a writable shared/system-style parent where logging can create the file without requiring parent chmod permission.


## Finding 4 (posted 2026-08-21T17:04:35Z)

**Adversarial Review — issue**

Client registration executes shell command strings built with an incomplete `shellQuote()`, so valid configuration values containing shell metacharacters can break setup and can execute unintended commands.

`defaultRun()` uses `child_process.exec(command)`, but `shellQuote()` only adds double quotes when a value contains whitespace or `"`:
```js
function shellQuote(value) {
    return /[\s"]/u.test(value) ? `"${String(value).replaceAll('"', '\\"')}"` : String(value);
}
```
Characters such as `;`, `&`, `|`, `$`, backticks, `<`, and `>` are therefore passed to the shell unquoted. Even the quoted branch is not a complete POSIX-shell escape because `$` and command substitution remain active inside double quotes.

Concrete reachable case: `GOOGLE_MCP_ENDPOINT` is user/config-file controlled and `validEndpoint()` allows `;`. With an endpoint such as `/mcp;id;:`, the generated Claude HTTP registration URL has no whitespace, so it is emitted raw into the `claude mcp add ...` command. Because `adapter.add()` feeds that string to `exec()`, the shell parses the semicolons as command separators instead of URL characters. The same quoting helper is also used for launch paths/args and Codex registration values, so legitimate install paths containing shell metacharacters can fail similarly.

This boundary matters more with the new config layer because cwd/user `.env` values now feed setup/lifecycle behavior directly.

Smallest fix / acceptance criteria: stop constructing registration commands for execution as shell strings. Use `execFile`/`spawn` with an argv array for automatic client operations, keeping shell-rendered strings only for display/manual copy-paste with a platform-correct quoting function. Add a regression test with an allowed endpoint/path containing `;`/`&`/`$` and verify it is passed as one literal argv value and no second command is interpreted.


## Finding 5 (posted 2026-08-21T17:05:51Z)

**Adversarial Review — issue**

The Codex HTTP fallback still prints the live bearer token, violating this PR's token-hygiene contract and #83's acceptance criterion that the fixed token be stored/referenced without being printed.

Reachable path:
1. Choose shared HTTP in setup with Codex installed.
2. If the Codex entry is missing or differs, `reconcileClientEntry()` takes the `adapter.name === 'Codex' && desired.url` branch because Codex cannot persist the token value in its MCP registration.
3. That branch builds `manualCommand` with either:
   - `export GOOGLE_MCP_HTTP_TOKEN='<live token>'`, or
   - `setx GOOGLE_MCP_HTTP_TOKEN "<live token>"`
4. `registerClients()` then prints `result.manualCommand` verbatim as the exact commands to run.

So the persisted bearer token is exposed in terminal scrollback precisely on the setup path the PR added. This contradicts the README/PR claim that the token is never printed, and issue #83 explicitly required the generated fixed token to be stored with restrictive permissions and never printed to stderr.

This is also avoidable: setup already knows the private token-file path. The manual instruction can reference/read that file without embedding the secret itself (or otherwise establish the launch environment without rendering the value).

Smallest fix / acceptance criteria: no setup/doctor/status/manual-command output should contain the bearer token bytes. Generate Codex completion instructions that reference the private token source rather than interpolating its value, and add a test asserting the rendered failure/manual path does not contain the test token.


## Finding 6 (posted 2026-08-21T17:06:19Z)

**Adversarial Review — issue**

The new client-entry backup can persist secrets that do not happen to have `token|secret|password|authorization` in their key name, and on a returning stdio setup it can create that backup with default file permissions inside an older non-private config directory.

`backupClientEntry()` writes the captured existing entry through:
```js
function redactEntry(value) {
  ...
  /token|secret|password|authorization/i.test(key) ? '[REDACTED]' : redactEntry(item)
}
```
That heuristic does not redact common credential keys such as `GOOGLE_MAPS_API_KEY`, `API_KEY`, `AWS_ACCESS_KEY_ID`, or arbitrary provider-specific credential names. A concrete repo-native case is an existing google MCP stdio entry with `env: { GOOGLE_MAPS_API_KEY: '...' }`: replacing that entry writes the actual Maps API key into `client-config-backups.log`.

There is a second problem on upgrades: `backupClientEntry()` does only `mkdir(configDir, { recursive: true })` and `appendFile(...)` with default modes. An existing pre-fix config directory can still be `0755`; a returning user with a valid old OAuth token who chooses stdio does not pass through the new token/HTTP writers that chmod the directory. The backup can therefore be created as a normal umask-derived `0644` file under that traversable directory.

Smallest fix / acceptance criteria: treat captured client config as secret-bearing by default. At minimum redact all environment-variable values and authorization/header values (prefer an allowlist for values safe to retain), sanitize credential-bearing URL fields, ensure the config directory is `0700`, and create the backup file as `0600`. Add a regression test with an existing entry containing `GOOGLE_MAPS_API_KEY`/`API_KEY` and a pre-existing `0755` config directory; neither the secret bytes nor permissive backup mode should survive.


## Finding 7 (posted 2026-08-21T17:08:11Z)

**Adversarial Review — issue**

The Codex adapter cannot recognize the current `codex mcp get google --json` output shape, so a correctly configured Codex entry is treated as different on every setup/doctor run.

`normalizeClientEntry()` currently does:
```js
const source = entry.config && typeof entry.config === 'object' ? entry.config : entry;
const { name, serverName, token, ...rest } = source;
if (!rest.command && !rest.url) return null;
```
But current Codex serializes both stdio and HTTP settings under a nested `transport` object. The upstream `run_get(... --json)` output is shaped like:
```json
{
  "name": "google",
  "enabled": true,
  "transport": {
    "type": "stdio",
    "command": "...",
    "args": [...],
    "env": {...}
  },
  ...
}
```
or `transport: { type: "streamable_http", url, bearer_token_env_var, ... }` for HTTP. This is the current OpenAI Codex implementation: https://github.com/openai/codex/blob/41ab01a2eaff4d4c0fc88d56a0027d1244c33e82/codex-rs/cli/src/mcp_cmd.rs

Because neither `command` nor `url` exists at the top level, `normalizeClientEntry()` returns `null` while `parseClientEntry()` still reports `status: 'found'`.

Concrete effects:
- A correct Codex stdio entry never compares equal, so setup offers to remove/re-add it every run instead of converging.
- `doctor` reports a correct Codex entry as different/problematic.
- A correct Codex HTTP entry also fails equality; the later Codex HTTP fallback then reports setup incomplete and prints manual commands even though the registration is already right.

The current tests mock `adapter.get()` with already-flattened entries, so they do not exercise the real Codex JSON contract.

Smallest fix / acceptance criteria: add a Codex-specific normalization step that extracts the supported fields from `entry.transport` (and ignores Codex metadata such as `enabled`, timeouts, tool filters, etc.) before comparing to the desired entry. Add fixture tests using the actual current `codex mcp get --json` shapes for both stdio and streamable HTTP, and verify setup returns `unchanged` / doctor returns configured.


## Finding 8 (posted 2026-08-21T23:04:36Z)

**Adversarial Review — issue**

Changing the configured HTTP bearer token can strand a healthy managed server and delete the only state needed to stop it.

Concrete failure scenario:
1. Start shared HTTP with the persisted token from `<configDir>/http-token`.
2. Before stopping/restarting it, set `GOOGLE_MCP_HTTP_TOKEN` to a new value (for example while rotating credentials).
3. `stopHttpService()` reads the recorded PID, sees that it is alive, then calls `getHttpServiceStatus({ env })`.
4. `getHttpServiceStatus()` prefers the new environment token, so both authenticated probes to the still-running server fail with the old token and return `diagnostic: 'unreachable-or-unauthorized'`.
5. `stopHttpService()` treats that as `foreign-or-unverified` **and deletes `http-server.json` without signaling the process**.

The old managed process is now still alive, but the CLI has discarded its PID/URL ownership record. A subsequent `restart`/`start` cannot stop it and will normally hit the occupied port, so token rotation requires manual process discovery/termination.

This is distinct from PID-reuse protection: the state was valid and the PID was live; only authentication changed. The current test `never signals a live foreign or unauthenticated pid` actually codifies the state deletion path but does not exercise the managed-server token-rotation case.

Smallest fix / acceptance criteria: on failed authentication/ownership verification, do **not** delete a live state record. Preserve it and return an explicit auth/token-mismatch diagnostic so the operator can retry with the existing token or an intentional recovery path. Add a regression test that starts a managed instance with token A, invokes stop/restart under token B, and verifies the process remains tracked (or is safely stopped through a purpose-built verified rotation flow) instead of becoming an untracked orphan.


## Finding 9 (posted 2026-08-22T00:04:30Z)

**Adversarial Review — issue**

`GOOGLE_MCP_HTTP_NO_AUTH=1` is a supported loopback mode, but setup and doctor still force Codex through the bearer-token path, so a valid no-auth HTTP configuration cannot converge.

Failure scenario:
1. Set `GOOGLE_MCP_HTTP_NO_AUTH=1` and run setup, choosing shared HTTP.
2. `configureTransport()` still calls `ensureHttpToken()` and returns a transport object with a token even though `resolveHttpAuthConfig()` has disabled authentication.
3. `buildClientEntry('Codex', { transport: 'http', ... })` always emits `bearer_token_env_var: 'GOOGLE_MCP_HTTP_TOKEN'`.
4. If the Codex entry is missing or differs, `reconcileClientEntry()` unconditionally takes its `adapter.name === 'Codex' && desired.url` branch and returns `unsupported-http-auth` instead of adding the URL. `registerClients()` then aborts setup. This happens even though the running server does not require any bearer token.
5. `doctor` has the same assumption: `createDoctorDesiredEntryResolver()` always flags Codex HTTP as a problem when the token is not inherited, so a manually configured `{ url }` entry against a healthy no-auth server is reported unhealthy.

The failure is specific to the no-auth mode this PR still exposes and documents; there is no authentication value Codex needs in this case.

Smallest fix / acceptance criteria: carry the effective `noAuth` setting into client-entry construction and inspection. For no-auth HTTP, build Codex as URL-only, skip the `unsupported-http-auth` branch/token-environment requirement, and let setup add or repair the entry normally. Add setup + doctor regression tests with `GOOGLE_MCP_HTTP_NO_AUTH=1`, no token environment variable, and a missing Codex entry.


## Finding 10 (posted 2026-08-22T01:00:19Z)

**Adversarial Review — issue**

Disabling the plain diagnostic log also disables structured JSONL even when `GOOGLE_MCP_JSONL_FILE` is explicitly configured, contradicting the documented independent controls.

Concrete failure scenario:
1. Set `GOOGLE_MCP_LOG_FILE=0` because only structured diagnostics are wanted.
2. Set `GOOGLE_MCP_JSONL_FILE=/some/writable/server.jsonl`.
3. `getStructuredLogFilePath()` begins with `const plainPath = getLogFilePath(); if (!plainPath) return null;`.
4. Because the plain log is disabled, the function returns before it ever examines `GOOGLE_MCP_JSONL_FILE`.
5. `logToolCall()` therefore never writes JSONL, and `readRecentToolCalls()` also reports no structured log.

The runbook explicitly says `GOOGLE_MCP_LOG_FILE` “changes or disables the plain file” and `GOOGLE_MCP_JSONL_FILE` “changes or disables JSONL,” and says to set either variable to disable that sink. The current tests always configure a live plain-log path alongside JSONL, so this combination is uncovered.

Smallest fix / acceptance criteria: resolve `GOOGLE_MCP_JSONL_FILE` independently. An explicit JSONL path must remain active when `GOOGLE_MCP_LOG_FILE` is disabled; only the JSONL variable should disable structured logging. Keep any default-path coupling only for the case where JSONL has no explicit path. Add a regression test with `GOOGLE_MCP_LOG_FILE=0` plus an explicit `GOOGLE_MCP_JSONL_FILE` and verify `logToolCall()` creates/writes the JSONL file while no plain log is created.


## Finding 11 (posted 2026-08-22T02:00:42Z)

**Adversarial Review — issue**

`google-tools-mcp stop` exits with status 0 even when it explicitly did not stop the recorded live process.

Concrete failure scenario:
1. `stopHttpService()` reads a live recorded PID but cannot verify ownership, for example because the service is unreachable/unauthorized or the PID now belongs to something else.
2. It returns `{ status: 'foreign-or-unverified' }` without sending `SIGTERM`.
3. `dist/index.js` treats every stop result except `stop-timeout` as success: `exitOperationsCli(result.status === 'stop-timeout' ? 1 : 0)`.
4. Shell scripts, service managers, and operators therefore receive exit code 0 even though the target is still running and the command's requested action did not occur.

The existing `never signals a live foreign or unauthenticated pid` test verifies that no signal is sent, but there is no CLI-level assertion that this non-stop result is a failure.

Smallest fix / acceptance criteria: return a non-zero CLI exit code for `foreign-or-unverified` (and any future status meaning the requested stop was not completed). Add an entrypoint test that exercises this status and asserts the process exits non-zero while preserving the no-signal safety behavior.


## Finding 12 (posted 2026-08-22T03:03:50Z)

**Adversarial Review — issue**

Changing a running shared HTTP service from `GOOGLE_MCP_HTTP_NO_AUTH=1` back to authenticated mode can silently leave the old unauthenticated server in place while `start` reports that it attached successfully.

Concrete failure scenario:
1. Start the managed service with `GOOGLE_MCP_HTTP_NO_AUTH=1`. `startV2HttpServer()` then accepts requests without checking a bearer token.
2. Remove `GOOGLE_MCP_HTTP_NO_AUTH` (or set it false) and run `google-tools-mcp start` again, expecting the configured profile to now require bearer authentication.
3. `startHttpService()` calls `getHttpServiceStatus()` using the *new* environment. That probe sends a bearer token, but the already-running no-auth server ignores token authentication and still answers successfully, so status is `healthy`.
4. `assertHttpServiceConfigurationMatch()` only compares `host`, `port`, `endpoint`, `profile`, identity, and version. The published state also carries no auth-mode field. There is therefore nothing that detects that the live process is still running with `noAuth=true` while the requested configuration is `noAuth=false`.
5. `startHttpService()` returns `{ status: 'attached' }` without restarting the process. The operator has asked for authenticated mode, but the full Google Workspace MCP surface remains reachable by any local process with no bearer token.

The inverse transition is also not represented in managed state, but this direction is security-sensitive because the requested hardening never takes effect.

Smallest fix / acceptance criteria: make the effective HTTP auth mode part of the managed service configuration/ownership comparison. Persist a non-secret auth descriptor such as `noAuth` in `http-server.json` (or verify equivalent live behavior), include it in `getHttpServiceConfigurationDifferences()`, and refuse attach when it differs so an explicit restart is required. Add a lifecycle regression test that starts a no-auth managed instance, requests authenticated mode, and verifies `start` does not report `attached` while unauthenticated requests still succeed.


## Finding 13 (posted 2026-08-22T03:59:55Z)

**Adversarial Review — issue**

`confirmPublicPost` does not confirm the draft the caller actually reviewed. The confirmation call rebuilds the issue body from a fresh set of arguments, so a caller can review one draft and publish different, unreviewed content or diagnostics on the next call.

Concrete failure scenario:
1. Call `feedback` with `confirmPublicPost: false`, `includeDiagnostics: false`, and description A. The tool returns the markdown draft and tells the caller to review it.
2. Call `feedback` again with `confirmPublicPost: true`, but description B or `includeDiagnostics: true`.
3. The tool does not compare the second request with the reviewed draft or require any draft identifier/hash. It recomputes `body` from the second call and immediately passes that new body to `tryGhCli()` / the browser fallback.
4. The public GitHub issue can therefore contain text or recent activity that was never present in the draft the user approved. In particular, diagnostics can be added only on the publishing call, bypassing the explicit "review before submitting" step this PR introduces.

The current implementation in `dist/tools/index.js` makes the review and publish operations two independent tool calls with no binding state:
- the first call returns `{ method: 'review-required', markdown: body }` when `confirmPublicPost` is false;
- the second call trusts `confirmPublicPost: true` and publishes whatever `body` that second call just generated.

Smallest fix / acceptance criteria: bind confirmation to an exact reviewed payload. Return a deterministic draft ID/hash (covering title, body, label, and diagnostics) with the review response, require that ID on the publish call, and reject publication if the regenerated payload does not match. Alternatively accept the exact reviewed draft as the publish input and verify its hash. Add a regression test that reviews with diagnostics off, then attempts to confirm with diagnostics on or changed text; publication must be refused until the changed draft is reviewed.


## Finding 14 (posted 2026-08-22T05:05:31Z)

**Adversarial Review — issue**

`GOOGLE_MCP_PROFILE` is used as an unchecked path component, so a profile containing `..` can move the config directory outside `~/.config/google-tools-mcp` and make this PR chmod/write unrelated directories.

Concrete failure scenario:
1. An operator sets `GOOGLE_MCP_PROFILE=../shared` to name a profile. The docs only say the variable selects the user config directory; there is no schema restricting it to a safe single path segment.
2. `dist/config.js` computes `configDir = path.join(configBaseDir, configuredProfile)`. That resolves filesystem operations under `~/.config/shared`, not under `~/.config/google-tools-mcp`.
3. This PR now routes diagnostics, HTTP state/token files, and OAuth token persistence through that shared `getConfigDir()`. Those writers call `mkdir(..., 0700)` and/or `chmod(configDir, 0700)` before writing.
4. Starting the server, status/lifecycle operations, logging, or saving credentials can therefore change permissions on an unrelated existing directory and write `server.log`, `server.jsonl`, `http-token`, `http-server.json`, or `token.json` there.

The traversal primitive existed in the old auth-only profile path, but this PR materially expands its blast radius by centralizing the path and adding unconditional directory chmods in the new logging/state/token writers.

Smallest fix / acceptance criteria: validate `GOOGLE_MCP_PROFILE` as a profile identifier, not a filesystem path. Reject absolute paths, `.`/`..`, path separators, and any value whose resolved directory is not a direct child of `configBaseDir`. Add a regression test with `GOOGLE_MCP_PROFILE=../shared` and verify startup/config resolution fails without reading, writing, creating, or chmodding the escaped directory.


## Finding 15 (posted 2026-08-22T06:02:23Z)

**Adversarial Review — issue**

The new always-on/customizable diagnostic logger follows an existing symlink at the configured log path, then chmods and appends to the symlink target. That makes a custom log path in a shared directory capable of modifying an unrelated file instead of creating a diagnostic log.

Concrete failure scenario:
1. Configure `GOOGLE_MCP_LOG_FILE=/shared/server.log` (or `GOOGLE_MCP_JSONL_FILE`) where another local user/process can write the parent directory.
2. Before startup, that actor creates `server.log` as a symlink to another file the MCP process can write.
3. `initLogFile()` calls `rotateIfAlreadyOversized()` and then `openPrivateLogFile()`. Both use APIs that follow symlinks (`statSync`, `openSync`), and `openPrivateLogFile()` then calls `chmodSync(filePath, 0o600)`.
4. If the target is under the rotation threshold, startup opens the target in append mode, changes the target's permissions to `0600`, and writes diagnostic lines into it. Structured JSONL has the same path through `openPrivateLogFile()`.

This is separate from the existing finding about chmodding a custom parent directory. Even after the parent-mode fix, the leaf path remains unsafe. `dist/httpState.js` already demonstrates the safer repository pattern: `lstat()` the leaf and refuse symbolic links/non-regular files before reading or writing them.

Evidence: current `dist/logger.js` at `220f97f` uses `fs.openSync(filePath, 'a', 0o600)` and `fs.chmodSync(filePath, 0o600)` with no `lstat`/`O_NOFOLLOW` check: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/logger.js

Smallest fix / acceptance criteria: refuse an existing log path unless `lstat()` says it is a regular non-symlink file, and create new files with an exclusive/no-follow-safe path so a race cannot swap in a symlink between validation and open. Apply the same rule to plain and JSONL logs. Add tests where each configured log path is a symlink to a sentinel file and verify startup neither chmods nor appends to the sentinel.


## Finding 16 (posted 2026-08-22T07:00:47Z)

**Adversarial Review — issue**

Runtime rotation of the structured JSONL log drops the `0600` permission guarantee this PR added. After the first in-process rotation, the replacement `server.jsonl` is recreated with the process umask instead of the private mode used at startup.

Concrete failure scenario:
1. Run the long-lived shared HTTP service with default diagnostics enabled. Startup calls `openPrivateLogFile(server.jsonl)`, so the initial JSONL file is `0600`.
2. Once `structuredBytes + lineBytes > rotationLimit`, `logToolCall()` calls `rotateNow(filePath)`. That renames the private current file to `server.jsonl.1` and leaves no file at `server.jsonl`.
3. The next line in the same function is `fs.appendFileSync(filePath, line, 'utf8')`. Because the path no longer exists, Node creates a new file using the normal append-file default mode (`0666` masked by the process umask), commonly `0644` under umask `022`.
4. `structuredLogPath` remains initialized, so `openPrivateLogFile()` is not called again to chmod the replacement. The long-lived process can therefore keep writing tool names, timing, argument shapes, and failure classifications to a world-readable JSONL file until restart.

The runtime-rotation test verifies that both files exist and contain data, but it never asserts the replacement JSONL mode after rotation. The plain logger does not have this bug because its rotation path explicitly calls `initLogFile()` before appending again.

Evidence at the current head: `dist/logger.js` rotates JSONL with `rotateNow(filePath); structuredBytes = 0;` and then directly `appendFileSync(...)`, while the `0600` hardening only happens inside `openPrivateLogFile()`: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/logger.js

Smallest fix / acceptance criteria: after rotating structured JSONL, recreate/open the replacement through the same private-file helper before appending, or pass an explicit `0600` mode through a no-follow-safe creation path. Add a POSIX regression assertion that the active `server.jsonl` remains `0600` after runtime rotation, not only at initial creation.


## Finding 17 (posted 2026-08-22T08:03:04Z)

**Adversarial Review — issue**

The new early config layer lets an arbitrary project-local `.env` disable shared-HTTP authentication or switch transports before the server starts. That turns a repository file in the current working directory into a security/lifecycle configuration source.

Concrete failure scenario:
1. A user opens/clones a project whose `.env` contains `GOOGLE_MCP_HTTP_NO_AUTH=1` (and optionally `GOOGLE_MCP_TRANSPORT=http`), with neither variable already set in the real environment or user config.
2. `dist/config.js` is imported as the first module in `dist/index.js`. It loads the user `.env`, then `process.cwd()/.env`, and copies every syntactically valid key into `process.env` when not already defined. There is no allowlist or restriction for security-sensitive keys.
3. `dist/index.js` and `dist/httpLifecycle.js` subsequently read that mutated environment. `google-tools-mcp start` therefore resolves `noAuth: true` from the project file, and a normal no-argument client launch can also be forced from stdio into HTTP by `GOOGLE_MCP_TRANSPORT=http`.
4. In the no-auth case the HTTP facade skips bearer-token validation, so the full Google Workspace MCP surface is reachable by any local process even though the operator never chose to disable authentication. This PR specifically moves config loading before transport/auth selection; the old auth-time `.env` loading happened too late to change startup transport/auth behavior.

The README now documents cwd `.env` as a general configuration tier, so this is reachable by design rather than an unsupported parser edge. Project repositories are also a materially different trust boundary from the user-scoped config directory.

Evidence at the current head:
- `dist/config.js` loads `path.join(process.cwd(), '.env')` and assigns every key into `process.env`: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/config.js
- `dist/index.js` imports that module before resolving transport/auth: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/index.js
- `resolveHttpAuthConfig()` trusts `GOOGLE_MCP_HTTP_NO_AUTH` from `process.env`: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/httpAuth.js

Smallest fix / acceptance criteria: do not accept security/lifecycle variables from lower-trust cwd/package `.env` files. Either restrict those tiers to an explicit safe allowlist, or make variables such as `GOOGLE_MCP_TRANSPORT`, `GOOGLE_MCP_HTTP_NO_AUTH`, host/port/endpoint, token/log/state-path controls, and profile-sensitive settings process/user-config-only. Add a regression test with a cwd `.env` containing `GOOGLE_MCP_HTTP_NO_AUTH=1` / `GOOGLE_MCP_TRANSPORT=http` and verify it cannot alter the startup security/transport configuration unless explicitly set in a trusted source.


## Finding 18 (posted 2026-08-22T09:05:11Z)

**Adversarial Review — issue**

Changing `GOOGLE_MCP_HTTP_ALLOWED_ORIGINS` while a managed HTTP service is already running can silently leave the old origin policy active while `start` reports `attached`.

Concrete failure scenario:
1. Start the managed service with `GOOGLE_MCP_HTTP_ALLOWED_ORIGINS=https://old.example`. `resolveHttpAuthConfig()` passes that allowlist into `startV2HttpServer()`, so the live handler permits that browser Origin.
2. Tighten the configuration to remove `https://old.example` (or replace it with a different allowed Origin), then run `google-tools-mcp start` again.
3. `startHttpService()` probes the existing process and, because it is healthy, calls `assertHttpServiceConfigurationMatch()`.
4. `getHttpServiceConfigurationDifferences()` compares only `host`, `port`, `endpoint`, `profile`, identity, and version. Neither the managed state nor the comparison contains `allowedOrigins`, so the change is invisible and `start` returns `status: 'attached'`.
5. The old process keeps its startup-time `allowedOrigins`, so a browser Origin the operator believes they removed remains authorized until an explicit restart.

This is distinct from the existing `GOOGLE_MCP_HTTP_NO_AUTH` finding: even with bearer authentication unchanged, the browser-origin gate itself can stay stale. The current lifecycle tests cover host/port/endpoint/profile/version differences but not origin-policy differences.

Evidence at the current head:
- `resolveHttpAuthConfig()` resolves `allowedOrigins` from `GOOGLE_MCP_HTTP_ALLOWED_ORIGINS`: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/httpAuth.js
- `getHttpServiceConfigurationDifferences()` does not compare it, and `createPublishedState()` does not persist it: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/httpLifecycle.js

Smallest fix / acceptance criteria: include a canonical, non-secret representation of the effective origin allowlist in managed HTTP state and configuration comparison (for example, normalized/sorted origins), and refuse attach when it differs so a restart is required. Add a lifecycle regression test that starts with origin A, requests a configuration without A, and verifies `start` does not return `attached` while the old process still accepts Origin A.


## Finding 19 (posted 2026-08-22T09:59:48Z)

**Adversarial Review — issue**

`troubleshoot` stops seeing the immediately preceding tool failures after JSONL rotation because `readRecentToolCalls()` reads only the new primary `server.jsonl` and never includes `server.jsonl.1`.

Concrete failure scenario:
1. A long-lived shared HTTP server accumulates tool-call records until `server.jsonl` crosses the runtime rotation threshold.
2. `logToolCall()` renames the full file to `server.jsonl.1`, creates/continues a fresh `server.jsonl`, and writes the newest call there.
3. The user immediately calls `troubleshoot` after a failure that was among the final records moved into `.1`.
4. `readRecentToolCalls(limit = 200)` reads only `getStructuredLogFilePath()` and slices that primary file. It never reads `${filePath}.1`.
5. `troubleshoot` therefore reports a recent-activity window containing only post-rotation records and can omit the failure the user is trying to diagnose, even though the retained `.1` file still contains it.

This contradicts the runbook's promise that `troubleshoot` summarizes recent activity and the last five failures. Rotation explicitly retains `.1`, but the reader does not use that retention. The current runtime-rotation test only asserts that `.1` exists and the new primary contains an `after` record; it never calls `readRecentToolCalls()` across the boundary.

Evidence: current `dist/logger.js` at `220f97f` implements `readRecentToolCalls()` with a single `fs.readFileSync(filePath, 'utf8')`, while `rotateNow()` retains the previous primary as `${filePath}.1`: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/logger.js . The documented behavior is in https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/docs/troubleshooting-runbook.md .

Smallest fix / acceptance criteria: when the primary contains fewer than the requested `limit`, read the retained `.1` as well, parse valid tool-call records from both in chronological order, and return the newest `limit` across the combined window. Add a regression test that forces runtime rotation, leaves fewer than five failures in the new primary, then verifies `readRecentToolCalls()` / `troubleshoot` still includes failures retained in `.1`.


## Finding 20 (posted 2026-08-22T11:06:54Z)

**Adversarial Review — issue**

`doctor` can report a persisted shared-HTTP setup as healthy without checking the HTTP service at all when no supported client entry is currently detected.

Concrete failure scenario:
1. A user previously chose shared HTTP, so `GOOGLE_MCP_TRANSPORT=http` remains persisted in the user config.
2. Claude Code and Codex are not installed on the machine running `doctor` (or neither adapter is detected), while OAuth credentials and `token.json` are valid.
3. `dist/index.js` correctly resolves the requested doctor transport as HTTP and builds an HTTP desired-entry resolver.
4. `inspectSetup()` loops only detected adapters. With none detected, `clients` stays empty. It then decides whether to inspect the managed service with `const usesHttp = clients.some(client => client.entry?.url)`, which is false for an empty list, so `getHttpServiceStatus()` is never called.
5. With valid OAuth credentials/token and no config warnings, `problems` is empty and `doctor` returns `healthy: true` even if the persisted HTTP service is stopped, unreachable, unauthorized, or running the wrong process.

This contradicts the new `doctor` contract and PR description that it inspects shared HTTP health. The selected transport is already known before `inspectSetup()` runs; HTTP health should not depend on whether a supported client CLI happens to be installed.

Evidence at the current head:
- `dist/index.js` resolves HTTP before calling `inspectSetup()`: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/index.js
- `dist/setupInspect.js` gates `inspectHttp()` only on `clients.some(client => client.entry?.url)`: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/setupInspect.js

Smallest fix / acceptance criteria: pass the resolved transport (or an explicit `inspectHttp: true` signal) into `inspectSetup()` and always inspect managed HTTP health when the effective doctor transport is HTTP, independent of detected client entries. Add a regression test with persisted HTTP transport, zero detected adapters, valid OAuth state, and an unhealthy HTTP status; `doctor` must exit unhealthy and report the shared-service problem.


## Finding 21 (posted 2026-08-22T12:00:50Z)

**Adversarial Review — issue**

`google-tools-mcp start` loses the actionable `GOOGLE_MCP_PORT` collision error that this PR promises, because the detached child’s startup error is discarded and the parent reports only a generic readiness failure.

Concrete failure scenario:
1. A foreign process is already listening on the configured loopback port.
2. Run the documented primary lifecycle command `google-tools-mcp start`.
3. `startHttpService()` spawns `serve` with `stdio: 'ignore'`. The child’s `EADDRINUSE`/`GOOGLE_MCP_PORT` diagnostic therefore never reaches the operator.
4. The parent polls `getHttpServiceStatus()`. Because the child never publishes `http-server.json`, status remains `not-running` until the readiness timeout.
5. The parent then throws only `Shared HTTP service did not become healthy (not-running). See ...`, which does not identify the port collision or tell the operator to change `GOOGLE_MCP_PORT`.

The existing collision regression test exercises `ENTRYPOINT serve` directly, where stderr is still visible. It does not exercise `start`, even though `docs/http-mode.md` presents `start` as the normal managed-service command and states that a foreign process on the port fails with an actionable `GOOGLE_MCP_PORT` message.

Evidence: `dist/httpLifecycle.js` launches the detached child with `stdio: 'ignore'`, then converts readiness failure to the generic `Shared HTTP service did not become healthy (...)` error. `tests/httpOperations.test.js` checks the actionable collision message only through `serve`.

Smallest fix / acceptance criteria: make the managed `start` path detect/report a port collision itself, or capture a bounded startup failure channel from the detached child before detaching. Add an entrypoint regression test that occupies the configured port, runs `google-tools-mcp start`, and asserts a non-zero exit plus an error containing `GOOGLE_MCP_PORT`.


## Finding 22 (posted 2026-08-22T13:00:31Z)

**Adversarial Review — issue**

First-install setup can save the OAuth client ID/secret successfully and then immediately fail authentication because the new config loader snapshots `.env` files only once per process.

Failure scenario:
1. Start with no user `.env` and no `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` in the process environment.
2. `setup.js` imports `getConfigDir` from `config.js`, whose module-level `loadConfigFiles()` runs immediately and sets `filesLoaded = true` while the user `.env` does not yet exist.
3. The first-install wizard collects the client ID/secret and `mergeCredentialEnv()` creates `<configDir>/.env`.
4. The wizard then enters the OAuth step. `auth.js::authenticate()` calls `loadClientSecrets()`, which calls `loadConfigFiles()` again.
5. Because `filesLoaded` is already true, that call does not read the newly-created `.env`. The process environment was never updated with the credentials either, so `loadClientSecrets()` falls through to `credentials.json` locations and throws `No OAuth credentials found` on the normal wizard path.

This is visible directly in the current head: `config.js` eagerly calls `loadConfigFiles()` at module evaluation and makes subsequent calls no-ops; `runFirstInstallSetup()` writes the credentials only to disk; and `auth.js` relies on `loadConfigFiles()`/`process.env` to see `.env` credentials. The new setup tests exercise `mergeCredentialEnv()` and returning-user setup separately, but do not run the real first-install save-then-auth sequence in one process.

Smallest fix / acceptance criteria: after the wizard persists the credentials, make them available to the current process before invoking auth (for example, set `process.env.GOOGLE_CLIENT_ID` and `process.env.GOOGLE_CLIENT_SECRET` from the just-confirmed values), or provide a deliberate config reload path that can safely re-read the newly-created user file. Add a regression test that starts with no credential env/file, runs the first-install credential persistence and real `loadClientSecrets()` in the same process, and verifies the saved values are returned without requiring a restart.


## Finding 23 (posted 2026-08-22T14:02:18Z)

**Adversarial Review — issue**

`GOOGLE_MCP_ENDPOINT` accepts URL paths that normalize to a different pathname, so the managed service can start listening and then fail its own authenticated readiness probe because the client and server disagree on the endpoint.

Concrete failure scenario:
1. Set `GOOGLE_MCP_ENDPOINT=/a/../mcp`.
2. `validEndpoint()` accepts it because it only requires a leading `/` and rejects `?` / `#`.
3. The HTTP handler is created with the raw configured endpoint and compares request pathnames against that raw string.
4. The managed state path reconstructs `state.url` through `new URL(...)`; standard URL normalization turns `/a/../mcp` into `/mcp`.
5. `startHttpService()` then calls `waitForHttpService()`, which probes MCP discovery at the normalized `state.url` (`/mcp`). The running handler is expecting `/a/../mcp`, so the probe gets a 404 and `start` reports a readiness failure even though the child successfully bound the port.

Internal whitespace has the same class of bug: a configured path such as `/mcp test` is accepted but URL clients serialize it as `/mcp%20test`, while the handler keeps the raw endpoint string.

Evidence at the current head:
- `validEndpoint()` in `dist/httpLifecycle.js` accepts these paths and `resolveHttpServiceConfig()` builds the service URL from the raw endpoint: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/httpLifecycle.js
- `normalizeState()` in `dist/httpState.js` reconstructs the published URL with `new URL(...)`, which normalizes dot segments / escaping: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/httpState.js
- The HTTP facade routes by comparing the parsed request pathname with the configured `endpoint`: https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/mcpServer.js

Smallest fix / acceptance criteria: canonicalize the configured endpoint once before using it anywhere, or reject any endpoint whose canonical URL pathname differs from the supplied value. Store, advertise, probe, and route against that same canonical pathname. Add managed-start regressions for a dot-segment path and an internally spaced path so accepted values either work end to end or fail validation before spawning the service.


## Finding 24 (posted 2026-08-22T15:05:53Z)

**Adversarial Review — issue**

Default diagnostic rotation is not safe when multiple stdio MCP servers run at the same time, so normal multi-client use can delete retained logs and drop records at the rotation boundary.

Concrete failure scenario:
1. stdio is the default one-process-per-client mode, and every process for the same profile writes to the same default `server.log` / `server.jsonl` paths.
2. Each process keeps its own `plainBytes` / `structuredBytes` counters and independently decides when the shared file crossed the rotation threshold.
3. If processes A and B cross that threshold together, both call `rotateNow(filePath)`, which performs two uncoordinated filesystem mutations: first `unlinkSync(filePath + '.1')`, then `renameSync(filePath, filePath + '.1')`.
4. A can rename the full active log to `.1`; B can then delete that newly retained `.1` before attempting its own rename. If A has not recreated the active file yet, B's rename fails with `ENOENT` and its current log record is dropped by the caller's catch. If A has recreated it, B can instead rotate A's fresh replacement and still destroy the intended retained history.
5. The result is diagnostic data loss caused by the supported multi-process stdio topology, even though rotation is supposed to retain the previous file.

This is distinct from the existing JSONL permission and `troubleshoot`-across-rotation findings. Those happen within one process; this failure comes from multiple supported stdio processes concurrently rotating the same files.

Evidence: [`dist/logger.js` at the current head](https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/dist/logger.js) uses shared profile-wide paths and an unlocked `unlinkSync` + `renameSync` rotation sequence. The current [diagnostics tests](https://github.com/karthikcsq/google-tools-mcp/blob/220f97fb744289d5cc68943da28f6c2d88baa817/tests/diagnosticsLogging.test.js) only exercise a single logger process.

Smallest fix / acceptance criteria: make rotation safe across processes. Either serialize rotation with a cross-process lock/lease and re-check the active file under that lock, or give each stdio process its own diagnostic files and aggregate them for troubleshooting. Add a regression test with two processes sharing one diagnostics directory, force both across the threshold concurrently, and verify retained history is not deleted and neither process loses its current record.
